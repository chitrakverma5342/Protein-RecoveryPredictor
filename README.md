# Protein-RecoveryPredictor

This project started as me getting myself familiar with basic scikit learn supervised models such as Linear Regression, Decision Tress, Random forest and Logistic regression by creating this predictor that reads inputs that acts as independent variables from a stored CSV file in the repository.
